var fs = require('fs');
var mv = require('mv');
var im = require('imagemagick');
var path = require('path');
var randomstring = require('randomstring');
var uuid = require('uuid');

module.exports = {

    CreateImageRecord: function(options, done) {
        return done();
    },
    SaveImageContents: function(options, done) {
        return done();
    },
};